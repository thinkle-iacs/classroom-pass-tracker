# Emulator-only parameters (project "demo-pass-tracker"). Safe to commit: no secrets.
# Every param is listed so `emulators:exec` never stops to prompt for one.
SIS_MODE=fixture
ALLOWED_DOMAIN=innovationcharter.org
# Placeholders: fixture mode never calls Aspen.
ONEROSTER_BASE_URL=https://oneroster.invalid/ims/oneroster/v1p1
ONEROSTER_TOKEN_URL=https://oneroster.invalid/oauth/token
